---
aside: false
---

# commands.yml 生成器

使用此工具快速为你的 `commands.yml` 文件生成一条命令注册配置。

<CommandsYmlGenerator />

---

### 如何使用生成的配置？
1. 在你的插件项目中，在 `src/main/resources/` 目录下创建或打开 `commands.yml` 文件。
2. 点击上方的 **“复制配置”** 按钮。
3. 将内容粘贴到 `commands.yml` 并保存。
4. 注意：如果你有多个命令，可以将生成的内容依次追加到文件中。
