---
aside: false
---

# plugin.yml 生成器

使用此工具快速生成你的 `plugin.yml` 插件描述文件。

<PluginYmlGenerator />

---

### 如何使用生成的配置？
1. 在你的插件项目中，在 `src/main/resources/` 目录下创建一个名为 `plugin.yml` 的文本文件。
2. 点击上方的 **“复制配置”** 按钮。
3. 将内容粘贴到 `plugin.yml` 并保存。
