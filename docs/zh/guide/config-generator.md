---
aside: false
---

# 配置文件生成器

使用此工具快速生成你的 `config.conf` 配置文件。填写左侧表单，右侧将实时生成可复制的配置内容。

<ConfigGenerator />

---

### 如何使用生成的配置？
1. 在你的机器人 JAR 文件同目录下创建一个名为 `config.conf` 的文本文件。
2. 点击上方的 **“复制配置”** 按钮。
3. 将内容粘贴到 `config.conf` 并保存。
4. 启动机器人即可生效。
